export { KitchenInformation } from './kitchen-information.types';

/**
 * Tipo para bathroomInformation
 * Por ahora es un objeto flexible, se puede tipar más adelante
 */
export interface BathroomInformation extends Record<string, unknown> {}

/**
 * Tipo para basementInformation
 * Por ahora es un objeto flexible, se puede tipar más adelante
 */
export interface BasementInformation extends Record<string, unknown> {}

/**
 * Tipo para additionalWorkInformation
 * Por ahora es un objeto flexible, se puede tipar más adelante
 */
export interface AdditionalWorkInformation extends Record<string, unknown> {}

